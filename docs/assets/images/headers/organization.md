# Organization header sheet

Asset paths in this guide refer to `public/images/headers/` unless a repository-relative path is given.

Generated with the built-in `image_gen` tool on 2026-09-14 in one generation for both scenes. `public/images/headers/organization.png` is the approved true-alpha output, resized with macOS sips to 1024 × 512. Two scenes: left = daily-sequencing; right = categorization. The shared HeaderIllustration renderer uses measured crops on the 1024 × 512 sheet: left x27/y80/w489/h375; right x548/y57/w466/h419. Each crop fits inside 90% of a square, preserving proportions. Measured crops exclude small strokes crossing the nominal midpoint. Keep cells square and do not add a white theme backing.

References: `public/images/headers/daily-sequencing.png` and `public/images/headers/categorization.png` for scene identity; `public/images/headers/language-naming.png` as a working alpha-cutout reference. The old individual Organization files are reference sources only, no longer rendered. Earlier attempts with baked checkerboards were rejected.

## Final prompt

Create one cutout asset with a transparent background, matching the real transparency of reference 3. Include only the two scenes from references 1 and 2 in a side-by-side horizontal sheet. Left scene: lavender paper character arranging cards at table. Right scene: turquoise paper character sorting shapes in baskets. Preserve soft muted colors and handmade paper texture from references 1 and 2. No text. No background. Transparent PNG cutouts. Reference 3 is only an example of the required alpha transparency, do not include its book character.
